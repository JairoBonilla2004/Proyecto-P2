package ec.edu.espe.SecureFrameGallery.shared.enums;

/**
 * Proveedor de identidad del usuario.
 * Distingue entre registro local (formulario) y OAuth2 externo (Google).
 */
public enum AuthProvider {
    LOCAL,
    GOOGLE
}
